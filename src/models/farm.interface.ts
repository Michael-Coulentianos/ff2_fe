export interface Farm {
  farmId: number;
  farmIdentifier: string;
  partyId: number;
  partyIdentifier: string;
  organizationId: number;
  farm: string;
  name: string;
  azureUserId: string;
  organization: string;
}